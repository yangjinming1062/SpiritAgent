# 形象生成能力链

本文维护参考来源、形象能力编排、产物和失败恢复。

## 1. 参考与派生关系

```text
角色描述 + 可选参考图 → 头像种子 → 确认
                        ↓
                   全身种子（性格化 idle 姿态）
                               ↓ 确认并锁定身份
                      默认外观快照 → 双图角色卡 → 默认视频动作包
                               ├─ 后续换装 → 对应视频动作包
                               └─ 场景、自拍与其他出镜媒体
```

### 1.1 共用参考与种子图派生

| 输入或产物 | 必须保持的语义 |
|---|---|
| 头像种子 | 角色描述与可选图像生成，锚定身份；关系定位不作为外貌属性 |
| 全身种子图 | 由头像、角色资料和可选身体参考补全结构与比例，纯白背景、完整入画、稳定的性格化 idle 姿态 |
| 默认外观 | 全身确认时保存独立图片快照，不再生成中间立绘；默认视频直接从该快照生成 |
| 换装草稿 | 全量重绘以全身种子锚定身份与结构；视觉模型判断可穿戴部位、材质与姿态，不添加原本没有的器官 |

头像、全身、换装、场景与出镜媒体共用 [CHARACTER_VISUAL_STYLE](../backend/prompts/generation.py) 作为视觉风格的唯一提示词来源，生成、编辑与自备图提示词均引用它。具体风格只在该提示词内描述。提示词只写宽高比，像素尺寸由请求与供应商实现决定。

后端不设物种枚举、骨骼分类或性格候选词表。视觉 LLM 接收实际参考图、外貌、性格及反馈，判断身体结构、表面材质与待机姿态；没有视觉模型时明确失败。性格标签根据角色资料动态提炼，不将物种或身体结构当作性格。

带图提示词区分头像扩展、换装、进入场景和局部编辑。头像呈现参考只影响光线、色调与构图；双图经左右拼图传输，按图 1 / 图 2 说明分工。

服装、发型、发色、妆容与配饰属于可变造型，由外观描述承载。用户服装参考图先与文字整合为着装描述，不直接作为换装生图的身份参考；整合失败可按纯文字生成。场景和出镜媒体必须取得可读的全身种子图，缺失时停止并提示修复。

换装重绘把原设计与本次反馈分开传入身体判断和最终生图提示词；反馈只覆盖明确涉及的造型维度，其余沿用设计。全身重绘的身体判断同时参考当前着装，避免姿态建议按头像中的旧衣物生成。成品命名后的外观描述供桌面／视频形象与出镜媒体保持造型，须保留主要衣物、颜色、轮廓、发型与辨识配饰，不能只留下风格概括。

提示词按画面重点使用身份资料：头像、全身、换装及以角色为主体的出镜媒体注入角色卡；场景以环境与活动为主体，只用全身参考图保持固定身份，角色卡快照用于任务与启用校验。

场景环境与活动由 `notes` 承载；代码选择本次造型：非空白的 `outfit_description` 整段采用，否则读取衣柜已启用外观的描述，均缺失时省略着装段，不隐式继承当前场景或第二参考图的穿着。`outfit_description` 是完整造型而非局部补丁，可包含服装、配色、发型发色、妆容、鞋履与配饰；上游遇到局部修订时先以有依据的基础造型合并改动维度再提交，资料不足不补造整套服装。选定描述随任务保存并冻结进提示词，重试和恢复不重读另一套衣服，模型不接收来源或回退规则。场景参考图分工：图 1 只负责固定身份（面容、物种、身体比例、固有材质与标志性身体结构，服装发型配饰不笼统纳入）；图 2 负责环境布局、场景元素与构图线索，文字明确修改的维度优先，其中人物不进入成品、其穿着不构成默认造型。按实际参考数量装配说明，单图请求不输出图 2 条款。场景不反向修改外观，也不随后续换装重建；不附加固定室内陈设或默认房间光线。初始引导仅装配已明确提供、与环境或活动相关的少量性格或生活偏好事迹，无素材时使用简洁默认房间要求，不从外貌、物种或服饰推断兴趣、职业和经历；素材仅作用于初始引导，最终要求随任务保存。

场景先生成并保存图片，再基于实际图片分析简短标题和具体描述；描述采用客观第三人称画面描述，依据只来自实际图片（要求红衣成图蓝衣时记录蓝衣，遮挡细节不补造），供场景库浏览检索，并在启用后回灌对话模型理解环境、活动与可见穿着。生成要求与提示词独立持久化。分析失败保留图片，仅重试分析或由用户补全，不重复付费生图、不自动改写历史描述。齐备后成为可启用资产，是否自动启用按 [场景契约](PROTOCOL.md#12-伙伴生命周期方法方法级契约) 校验。

出镜媒体统一三个造型来源：衣柜已启用外观是默认造型；当前场景可见穿着是场景成品描述中的穿着信息；本次生成造型是实际传入这一次图片或视频的造型。出镜图片与视频接受显式 `outfit_override`（仅作用于本次产物，不修改衣柜或当前场景）：明确要求沿用场景穿着时，从实际场景描述整理可用造型，资料不足不虚构整套衣物也不伪称完全一致；无覆盖时沿用衣柜默认。同一生成任务冻结最终造型，重试和恢复不重新读取。图片提示词与视频首帧准备共同消费已选定造型；覆盖生效后不再复用不相符的衣柜图作优先穿着依据，必要时走角色卡校准流程。明确要求原样动画化已有图片时不传覆盖、省略 subject，保持原图语义。夜间出镜图片使用全身种子图与角色卡作身份依据，穿着读取衣柜已启用外观描述；夜间图片另按可用性携带衣柜图。出镜视频优先使用衣柜图；用户修订尚未应用、仅有着装描述或显式提供首帧时，先按本次角色卡与造型派生参考。恢复只续原视频任务。夜间媒体的 `narration` 生成独立音轨，不保证视频口型同步。

全身种子图重绘成功后才替换旧图，不自动重建既有外观或场景资产。

### 1.1.1 图像修改双模式（微调 / 重新生成）

`mode` 缺省为 `regenerate`，按各点位固定参考全量重绘。`edit` 使用上一版产物作为编辑底图，只改变反馈指定部分，必须同时满足：反馈非空、旧图存在且可读、未附加参考图、供应商声明并实现图像编辑能力。

编辑条件不满足时明确失败，不能静默改成全量重绘。身份条件化参考不等同于图像编辑能力；具体能力位与路由见生成模块。

### 1.1.2 用户自备图

自备图入口与按需获取制作资料的体验见 [DESIGN](DESIGN.md#53-孵化与头像获取)。外部提示词与内部生图共用参考来源及画面要求，必需参考缺失或不可读时同样失败；提示词不引用产品内部概念或供应商专有参数。采纳跳过生图，进入对应点位的存储与确认流程，不区分微调与重新生成，也不宣称程序完成身份验证。

外观确认接口不携带图片载荷：确认把草稿立绘转正为持久参考图（`ready`），只表示参考图就绪，不触发任何生成，也不自动穿着；穿着经激活接口显式完成。设计草稿阶段即可获取自备图提示词。

场景获取提示词时创建等待上传的记录，不启动生图；直接上传可跳过制作资料。采纳保存图片并启动描述分析，不自动启用；取消不清除当前环境。

场景付费请求提交前持久化标记，返回后保存结果地址。重启仅恢复已知结果的转存或本地图片分析；等待上传及结果未知的任务不提交生图。

### 1.1.3 角色卡与固定外形

全身确认时登记持久化分析任务。头像只提取头部特征，全身图只提取身体特征；结构和提取限制见 [角色卡模型](../backend/modules/companion/character_card.py)与 [CHARACTER_CARD_EXTRACTION](../backend/prompts/generation.py)。

双部分完整成功后原子发布，再启动初始资产。来源未变的失败任务只补失败部分；重启恢复中断任务，模型等待有超时且不持有数据库事务。回写核对身份、源路径和任务批次。重新提取期间保留已发布资料与来源指纹，成功时合并最新用户覆盖。

`appearance` 只用于初始化。确认后以角色为主体的生成与运行时人设使用角色卡：代码先合并自动特征和用户覆盖，下游提示词只接收生效特征，未描述的细节参照图片；换装、场景和动作要求不得修改固定特征。场景的参考使用遵循前述按画面重点的规则，不直接用角色卡文字修订参考图外貌。日常重绘不反向更新角色卡。

生成冻结本次资料，保存角色卡不重绘既有资产。全身及外观重绘写入、外观草稿确认均核对修订号，拒绝过期产物；场景和视频旧任务可完成为历史资产，但不自动激活。自备图替换草稿时清除校准标记，后续视频按当前资料校准。

## 2. 视频动作包链

视频包是同一外观的一组独立动作。建包时复制参考图并冻结角色卡，来源指纹包含身份 ID、外观 ID、图片路径与字节。未应用的用户修订先校准到参考图，再生成动作；明确失败可重试，结果未知或校准中断时不重发。参考就绪后不可变，全部动作共用其材质、身份、身体结构与造型；应用新资料须生成完整新包。视觉一致性仍需逐动作预览，不能仅凭共享输入保证。

默认外观在全身确认时与身份锁定同事务保存；角色卡发布后启动第一套视频包。首次建包同时保存默认视频已启动的事实；重复确认或重启只补从未启动的默认外观，删除视频包不撤销启动记录。启动失败不回滚形象确认，保存原因供界面展示；迟到失败不能覆盖已启动状态。建包后的失败与恢复统一遵循 [版本、恢复与删除](#23-版本恢复与删除)。

### 2.1 逐动作生成

1. 视觉 LLM 根据冻结全身参考图、角色资料、性格标签、外观描述和用户反馈，为请求动作编写起始姿态与一段动作描述，只表达可见的姿态、节奏和神态，不包含产品用途、镜头或背景要求。待机沿用种子或外观参考已有的姿态与神态，身体几乎不动；只有具备相应器官时才安排极轻微眨眼或呼吸。移动按实际结构选取步态、游动、爬行或飞行，拖拽为整个身体悬空，不强制双脚离地。代码固定动作集合及各动作时长（待机一秒，其余两秒），脚本快照逐动作保存。
2. 待机直接复用冻结参考作为关键帧，不再重绘姿态；其他动作由图像编辑链派生关键帧，保持身份和共享风格；关键帧持久化后才提交视频，避免把短片大部分时间用于从站姿进入动作。
3. 编排层将 LLM 的动作描述与固定视觉约束组合为完整视频提示词：身份与画风保持、循环首尾一致、固定镜头、全身留白、原地运动及纯净静态背景；待机另约束初始姿态、已有附属结构和视线方向不变。提示词只描述画面，不传入桌面或容器等产品实现概念。**短动作只使用 Grok 1.5**：动作关键帧同时作为首帧与末帧，冻结角色图另作身份参考，按各动作时长提交 720p 视频。MiniMax 不参加该路径的回退；聊天视频仍使用其原有供应商链。Grok 首尾帧 / 参考图模式的分辨率上限与请求映射见 [适配器](../backend/services/infrastructure/llm/providers/grok/video.py)。
4. 下载原始素材，语义抠像，检查可见性、透明背景和首尾接点，按动作时长上限选取至少一秒的循环片段；一秒待机支持恰好 24 帧的素材，不要求额外端点帧。不按时间或运动量猜测、拆分不同动作，也不靠倒放伪造步态。
5. 统一画布与帧率，编码透明 VP9/WebM（无音轨）；从最终编码文件提取封面和命中遮罩，校验后发布不可变版本。四个必需动作全部成功才可激活；新包失败保留旧形象。

当前供应商接口没有已实现的原生 alpha 视频输出能力，真实 Grok 输出为不透明 MP4，不能靠提示词中的“透明背景”替代抠像。导入已有透明素材时保留原生 alpha。

### 2.2 透明化、循环与消费

[matting](../backend/services/infrastructure/video_processing/matting.py) 使用 ISNet 通用前景模型保护浅色皮肤、衣物及毛发，不采用全局色键。模型由管理员放置于 `data_dir/models/<matting_model>.onnx`，缺失时在付费生成前拒绝。静止且遮罩变化较小的区域做有限时域混合，移动边缘不用历史遮罩拖尾；均匀背景下仅对语义边缘去混色，实体内部颜色保持不变。中间产物使用 FFV1/BGRA。

[quality](../backend/services/infrastructure/video_processing/quality.py) 检查逐帧覆盖率、边界残留及首尾前景颜色、轮廓和速度差异；不合格动作保留源视频并单独报告失败，不自动重复付费生成。数值门槛只能拒绝明显缺陷，不能证明动作语义、身份、细发丝或所有循环的主观质量。

WebM 的 alpha 存于 Matroska 辅助数据；读取时必须显式使用 `libvpx-vp9`（VP8 使用 `libvpx`），默认原生解码器可能丢弃 alpha。交付同时检查辅助数据和实际解码的透明 / 实体像素，不以容器标签为验收依据。上传路径支持可解码 alpha 的 WebM、FFV1/MKV、ProRes 4444/MOV 等素材；透明文件仍需存在真实前景与背景。

描述符契约见 [manifest](../backend/services/application/generation/video/manifest.py)：只携带不可变资源路径与哈希、画布、动作映射、逐帧 alpha 命中遮罩及必填采样帧率等调度信息。客户端双播放器始终挂载，备用片段开始播放且收到真实视频帧回调后才切换，短暂淡入淡出；解码、播放和首帧等待均可取消且受超时限制，失败保留可用片段，初始播放失败进入渲染降级。命中按实际播放器时间采样，并扣除 `object-contain` 留白。移动与拖拽由容器位移表达，不驱动嘴部或视线。

### 2.3 版本、恢复与删除

一个动作对应一条任务，独立保存阶段、脚本、关键帧、供应商句柄、源视频与处理结果。成功动作可预览；重做单动作创建新包版本并共享其余动作与冻结参考。续跑和单动作重做不重新读取当前外观图，参考图已变化的旧包仍可完成，但不会自动激活。共享文件在最后一个包引用消失后才回收，删除旧版本不能损坏新版本。

视频提交先保存阶段，成功取得任务 ID 后立即落库。重启后已有句柄按提交时模型续轮询、已有姿态图续提交、已有视频续处理、已成功动作直接复用；全部动作成功后发布失败可单独重试发布。视频提交中但无句柄，或姿态生成中但无产物，均视为结果未知，不自动重发。图像编辑为同步接口，没有可查询的异步句柄。继续处理前检查参考仍有效，不重做已成功动作；明确重做动作才重新请求生成。上传源只存在临时工作区，处理中断后须重新上传。

同一用户只允许一个构建中的包，发布与激活在用户锁内完成。上传包的参考已变时拒绝发布；生成包核对参考与角色卡后才自动激活，构建期间切换外观不抢回穿着。启用包同步启用对应着装。动作包状态及恢复入口见 [PROTOCOL](PROTOCOL.md#110-视频动作包方法)。

## 3. 渲染与传输

桌面消费视频动作包 manifest 与透明片段。签名轮换不代表内容变化，资产访问、缓存和恢复遵循 [PROTOCOL](PROTOCOL.md#15-资产-url-签名与传输缓存)。显示降级顺序只在 [DESIGN](DESIGN.md#12-形象降级与兜底体系永不空白) 定义。

## 4. 按改动选择验证

参考与提示词变更检查双图提取分工、身份与造型边界、用户覆盖优先级、输入顺序及派生失效；覆盖部分失败重试、并发编辑、来源变化、重启与旧视频快照。编辑与自备图变更检查前置守卫和采纳后的状态一致性。视频处理变更检查真实透明片段（编码、切分、遮罩），不能仅凭能力声明通过验收；抠像与循环策略变更须检查浅色主体、透明背景、坏接点和真实供应商样本；交付产物还须检查 Chromium 实际透明合成。

恢复变更覆盖提交未落库、轮询中断、下载失败、未知结果及迟到结果。可加载性、视觉质量、模拟结果和真实供应商结果分别报告。

仅修改文档时核对事实与链接，不要求重新触发付费生成。验证入口见 [Scripts](../scripts/README.md)。

## 5. 参考实现

[生成模块](../backend/services/application/generation/README.md)

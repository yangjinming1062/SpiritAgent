# Client

## 1. 职责与边界

桌面形象载体与可信本地枢纽：负责窗口、渲染、凭据、协议中转、Runner 生命周期和自更新；对话推理归后端，本机工具执行归 Runner。全局边界见 [ARCHITECTURE](../docs/ARCHITECTURE.md)，体验见 [DESIGN](../docs/DESIGN.md)。

本文维护主进程与各窗口共同遵守的协作约束，内部实现按任务进入对应模块。

| 修改内容 | 阅读入口 |
|---|---|
| 凭据、IPC、窗口生命周期、Runner、配置同步和磁盘缓存 | [主进程](main/README.md) |
| 界面、模块接缝、会话、语音、角色和渲染资源 | [渲染层](renderer/README.md) |
| 多窗口连接、主题首帧、形象与历史缓存 | [跨进程协作](#4-跨进程协作) |
| IPC 通道、载荷及运行时常量 | [共享契约](shared/ipc/) |
| 检查、构建、图标与发布 | [scripts](../scripts/README.md) |

## 2. 设计意图

- 伙伴呈现层与可信枢纽共享主进程，但渲染层只经 IPC 使用凭据与 Runner 能力，避免体验代码侵入安全边界。
- 生活空间与工作台复用对话表面及同一连接的数据流；切换窗口不应中断回合，各窗口运行时状态仍需分别水合。
- 已就绪形象优先从本地缓存呈现，新字节就绪后替换，网络失败不能抹掉当前形象；具体缓存与降级约束见 §4。
- 窗口、动画与播放属于客户端投影；打扰档位的权威归属见 [ARCHITECTURE.md §5.1](../docs/ARCHITECTURE.md)，产品规则见 [DESIGN.md](../docs/DESIGN.md)。

## 3. 架构地图

- [main/](main/README.md)是可信主进程，持有凭据、窗口与 Runner 生命周期；[renderer/](renderer/README.md)承载各窗口体验，通过 preload 调用主进程能力。
- [shared/ipc/](shared/ipc/)定义双方共享的通道与载荷；`renderer/shared` 是渲染侧共享层，依赖窗口环境的实现不能被主进程导入。
- 主进程与 preload 由 [tsup](tsup.config.ts)构建，渲染层由 [Vite](vite.config.ts)构建；preload 的 CJS 格式约束归[主进程 README](main/README.md#包边界)，内部依赖分别由子模块维护。

## 4. 跨进程协作

### 连接与设备就绪

- 网关宿主—代理：桌面精灵宿主持唯一 WS，其他窗口经主进程代理共享连接，避免各自连接互相顶号及复制重连逻辑。宿主独占 Runner 工具执行，前台表面负责语音；每个窗口连接后自行拉取斜杠目录，内存状态不能跨窗口直接共享。
- 设备就绪汇总：宿主结合网关连接、Runner 握手与能力同步发布执行资格；撤销契约见 [PROTOCOL §1.2](../docs/PROTOCOL.md)。Runner 断开、崩溃或开始停止时桥立即作废能力缓存，宿主发送空表；空表阻止新派发，已派发调用仍按既有结果与超时规则收尾。重连须先推配置再拉取工具、恢复运行态；断连、停止与新握手使旧能力查询和宿主同步失效，不能被迟到结果重新注册。恢复结果按原 `call_id` 查询，不因响应丢失重新生成调用标识。
- 连续性：序列号去重、确认与重放见 [PROTOCOL §0](../docs/PROTOCOL.md)；普通会话切换不重置水位，服务端重启或序列失同步才重置。

### 窗口与主题

- 冷启动主题播种：窗口 `loadURL` 前主进程从配置镜像取当前主题写进入口 URL 查询串，渲染层 theme store 模块加载时优先消费该参数（消费后即从地址栏剥离），本地 localStorage 为即时缓存，云端水合广播为最终收敛；镜像缺失或损坏回落默认主题，不阻塞开窗。入口 HTML 的静态主题声明是数据异常时的兜底浅色，不承担同步职责。
- 主进程集中裁决生活空间 / 工作台互斥开窗并保存上次入口；工作台精灵和工位位于同一物理窗口，避免跨窗口同步漂移。表面打开时收起桌面舞台、暂停桌面走位；产品布局见 [DESIGN §6.1](../docs/DESIGN.md)。
- 透明精灵须同时设置窗口透明与首帧 body 透明，否则背景覆盖桌面。生活空间 / 工作台用无边框透明外壳，CSS 负责圆角和阴影；系统亚克力矩形底会污染圆角和伴工外侧透明区。
- 鼠标穿透只有窗口级开关：精灵和弹层登记可见矩形，由统一命中机制切换捕获；弹层不能整窗抢占，否则其他桌面应用不可操作。工作台仅可见本体及标题栏参与整体拖拽。
- 单显示器接力：精灵窗只覆盖当前显示器工作区，跨屏时平移新旧原点差，拖拽基准不动；判别在途指针所属坐标空间，避免二次平移。保存窗口原点与精灵位置，恢复先贴屏再定位；拔屏回最近屏，避免全虚拟桌面带来的 DPI 与命中复杂度。
- 跨窗口附件经主进程信箱转交，再由目标窗口消费；各窗口内存不共享，不能只把附件写入来源窗口的 store。入口选择归 [DESIGN §6.3](../docs/DESIGN.md#63-用户直接交互戳拖悬停)，渲染侧生命周期归[会话模块](renderer/README.md#conversationmodulesconversation)。

### 资产与历史缓存

- 头像用主进程磁盘缓存，2D PSD 叠加 OPFS，3D GLB 用磁盘及 OPFS；衣橱另持久化列表元数据，立绘与描述符、PSD、扶边纹理共用主进程磁盘缓存；首次读取列表后顺序预取资产包，按磁盘预算淘汰。内容哈希命中直接读盘，衣橱不可变路径通过 `preferCache` 同样跳过网络，普通无哈希请求仍可条件刷新。旧缓存先呈现，新字节就绪才替换；网络失败保留旧形象，鉴权失败走会话过期，登出清除渲染及主进程形象 / 模型缓存。
- 会话历史本地秒开 + 增量同步：主进程按用户维度磁盘缓存每会话消息快照；打开/切换会话先渲染本地缓存，再以快照末条消息 id 作 `after_id` 走 `session.resume` 增量追上，锚点失效回落全量；IM 会话例外，恢复始终全量水合（帧重放与 `after_id` 增量不适用），以清除已消费消息的排队标记，见 [PROTOCOL §1.7](../docs/PROTOCOL.md#17-im-通道桥接apichannels)。`last_seq` 仅在重连且聊天列表为活数据时发送（帧重放水位），快照 seq 不得作 `last_seq`——快照不追踪实时回合，陈旧水位会让服务端重放帧重复追加。压缩/撤回/清空的全量 hydrate 覆盖本地快照；删除会话同步移除其快照；登出与换号由主进程随资产缓存一并清空。对话媒体经 `preferCache` 命中主进程资产磁盘缓存，永久资产不再重复条件请求。契约见 [PROTOCOL §1.8](../docs/PROTOCOL.md)。

## 5. 与外部的契约

- 后端与 Runner 的生命周期、事件、配置、凭据和更新见 [PROTOCOL](../docs/PROTOCOL.md)；Skills 的平台声明和过滤见 [§2.6](../docs/PROTOCOL.md#26-skills-平台声明与过滤)。
- 3D / 2D 产物与动画映射见 [PIPELINE](../docs/PIPELINE.md)，打扰档位权威见 [ARCHITECTURE §5.1](../docs/ARCHITECTURE.md#51-打扰档位约束)。
- 图标母图及安装器同步导出见 [scripts](../scripts/README.md#9-共享图标资源)。

## 6. 验证入口

依赖安装和开发启动见[项目入口](../README.md#从源码启动)，lint、类型检查与构建命令见 [scripts](../scripts/README.md#8-按改动选择验证)。IPC 变化需同时核对共享类型、preload、主进程处理器与渲染调用方；多窗口或异步状态变化需检查切换、断连、登出与迟到结果。透明合成、GPU 恢复、系统快捷键与本机能力仍需在受影响平台验证，静态检查不能代表桌面验收。

{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Capabilities required by SpiritAgent Setup. Narrowly scoped: we don't write user files outside SPIRITAGENT_HOME, we don't read arbitrary paths, and we make no external network calls (the install script is bundled into the binary itself).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:event:default",
    "opener:default",
    "dialog:default",
    "process:default",
    "shell:default"
  ]
}

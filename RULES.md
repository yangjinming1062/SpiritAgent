# RULES.md

本文件规定项目协作、文档、提示词、代码与验证要求。具体产品行为和架构约束由对应文档维护，机器可检查的规则以仓库配置为准。新增全局规则前，先判断现有条款、模块文档或自动检查能否覆盖，避免为一次性问题不断追加要求。

## 协作规范

1. 按 [AGENTS.md](AGENTS.md) 定位任务入口，阅读相关模块的边界与约束；局部修正不要求通读无关文档。
2. 动手前明确目标、影响范围和验收依据。先查文档与代码；只有无法合理推断且影响目标、公共契约、数据安全或授权的信息才需要询问。一般实现选择遵循项目惯例，重要假设须说明。
3. 在已授权范围内连续完成实现、验证和必要修复，不逐步重复确认。不覆盖或撤销已有的无关工作区改动；超出当前授权的发布、历史重写及数据删除等操作须先确认。

## 文档维护规范

### 读者与写作目标

根 README 面向使用者介绍产品、演示和快速开始。工程文档主要帮助 AI-Agent 定位代码、理解设计、遵守约束和验证改动，不重复产品宣传或逐函数解释实现。

保留维护时容易遗漏的信息：职责边界、设计理由、跨文件约束、失败与恢复语义、已确认限制和验证入口。字段、签名、枚举及默认值链接源码定义；必要时用最小示例解释含义。

### 文档职责与事实归属

| 文档 | 维护内容 |
|---|---|
| [README.md](README.md) | 产品介绍、演示、使用与快速开始 |
| [AGENTS.md](AGENTS.md) | Agent 阅读路由；其他 Agent 入口引用它 |
| [RULES.md](RULES.md) | 项目协作与维护要求 |
| [ARCHITECTURE.md](docs/ARCHITECTURE.md) | 模块、依赖、状态归属、信任边界和平台支持 |
| [DESIGN.md](docs/DESIGN.md) | 产品体验、设计理由及故障表现 |
| [PROTOCOL.md](docs/PROTOCOL.md) | 跨模块通信、交付、状态、安全与兼容契约 |
| [PIPELINE.md](docs/PIPELINE.md) | 形象生成流程、供应商能力、产物与恢复要求 |
| 模块 README | 模块职责、内部约束、任务导航与独有操作 |
| [scripts/README.md](scripts/README.md) | 仓库级检查、调试、构建与发布入口 |

同一事实只维护一份完整定义，其他位置简述并链接。行为或契约变更时只在上表归属文档写完整定义，不在注释或多个文档重复展开。跨主模块契约写入对应主题文档；模块内共享约束写入最近的共同父级 README，局部实现限制留在所属模块。

代码与配置定义实际执行结构，文档解释约定语义与设计理由。两者冲突时结合任务要求、既有约定和实际消费链核实，不默认任何一方必然正确，也不通过修改文档掩盖实现缺陷。

### 写作与同步

1. 描述当前行为，只描述现状，不叙述修改历史或变化过程；写清必要的条件、例外和失败后果。仍有效的迁移要求与限制不能为缩短篇幅删除；计划、假设和审查结论须标明状态与依据。
2. 正文使用中文，产品术语保持一致，标准技术名词保留原名；路径和代码标识符用反引号。DESIGN 侧重体验与理由，实现细节链接技术文档。
3. 描述精简，按实际内容组织标题、段落、列表或表格；不规定固定章节数、行数或图表数量，不为套模板补写空洞内容。
4. 不是所有修改都有必要计入文档：改动影响文档事实、约束或导航时，在同一变更中更新定义与引用；移动内容或调整标题后核对路径、锚点和章节引用。

### 子模块 README 规范

按需说明模块负责什么、关键入口在哪里、哪些约束容易改错，以及改动后需要同步和验证什么。设计取舍说明真实理由，不编造替代方案；已知限制写清影响和必要处置。

父级 README 保留共享约束和导航，子包只展开独有内容。存在独立维护需求时才新建 README，不要求每个目录都有文档，也不填写无实际内容的占位章节。

安装载荷中的 SKILL.md、第三方资料与许可证遵循各自格式，不套用模块 README 要求。

## 提示词设计与修改规范

适用于项目内模型指令的设计、修改与排障。场景行为及输入输出契约由对应模块维护，不强制统一模板。

1. 定义模型要完成的任务、输入用途、输出消费方式和成功标准，区分硬约束、默认行为与自主判断空间，不用抽象形容词代替可判断的要求。
2. 以实际发送的消息、工具和多模态输入为依据。区分指令与资料及其权限，不假定模型看过未提供的内容；资料和工具返回不能自行扩大授权。
3. 按任务保留必要信息、条件与例外，合并重复要求并消除冲突。示例只用于消歧义，不把偶然细节变成规则；共享要求集中维护并正确装配。
4. 模型负责语义判断与生成，代码负责权限、状态、预算和确定性校验。影响生成的约束仍须告诉模型，输出必须符合接口及消费端能力，不能把推测或未完成操作写成事实。
5. 从实际输入、完整请求、原始返回及下游处理定位根因，区分指令、资料、实现与能力问题。只修改相关内容，修改后复核完整请求；整体复核不等于全部重写。
6. 按风险比较修改前后的正常、失败及相关边界场景，确认结果能被正确使用。静态检查、模拟或单次成功不足以证明真实质量稳定改善；复验资料和交付说明遵循下文验证规范。

## 代码规范

### 原则一：控制修改范围

实现任务及必要联动，不夹带无关功能、重构或格式调整。接口、配置和数据结构变化须同步相关生产方、消费方及必要迁移，不能以“最小修改”为由遗漏完整交付。

遵循仓库配置与模块惯例，不照搬明显错误的既有写法。

### 原则二：保持实现简单

以正确性、可读性和维护成本选择实现，不追求最少行数。按实际职责拆分函数，不为假想复用添加抽象、开关或通用层；承担公共契约或资源管理职责的封装不能仅因代码短而删除。

公共契约和稳定数据结构显式建模。Python 函数参数与返回值标注类型；其他语言遵循相应类型检查要求，明确可推断的局部类型无需重复声明。不用宽泛类型或强制断言掩盖契约问题。

注释解释不明显的约束、原因和副作用，保持精简并只描述现状，不记录修改历史；跨文件设计写入模块文档。性能优化依据实际规模与瓶颈，不为形式上的“高效写法”牺牲清晰性。

### 原则三：保持模块边界

遵守架构与模块文档规定的依赖方向，跨模块通过公共入口访问，不新增依赖环，也不用延迟导入或转导出掩盖结构问题。

共享代码放入符合依赖方向的最小公共模块，不能因目录位置相近就让底层反向依赖业务层。入口负责公开契约或装配，业务逻辑归所属模块；资源启动与关闭由明确的生命周期入口管理。

### 原则四：处理失败与资源生命周期

在能够恢复、转换错误语义或隔离失败的位置捕获异常，其余向上传播。保留诊断原因，不静默吞错、不把失败伪装为成功；按真实边界风险处理失败，不因尚未发生就认定不可能。

资源在成功、异常和取消路径上都须正确释放。后台任务明确归属，由所有者负责失败报告与停止；有限操作设置超时或预算，长期监听须可取消。涉及副作用的重试先核对幂等性与恢复语义。

### 原则五：管理依赖与安全边界

优先使用标准库与已有依赖；新增依赖说明必要性及兼容影响，使用项目既有工具更新并提交依赖清单和对应锁文件。

外部输入、模型输出及资源访问在相应信任边界校验，类型声明和提示词不能替代权限控制。密钥、令牌及未经授权的敏感数据不得写入源码、日志或验证材料；具体边界遵循架构和协议文档。

### 原则六：清理死代码与兼容负担

删除本次产生或属于任务范围内、已确认无效的代码。不能仅凭文本搜索没有调用者就判定死代码，还须核对动态加载、注册入口和外部消费者。无关问题报告位置与依据，不夹带清理。

内部重命名同步更新调用者，不用别名代替必要修改。公共接口、持久化数据及跨版本消费者需要兼容时，明确适用对象、迁移方式和移除条件。

### 原则七：伙伴相关内容由大模型动态给出

伙伴台词、心情和主动表达由模型结合情境生成，程序不得用静态模板冒充动态表达。未取得内容时显示真实状态或使用已约定降级，不编造伙伴语句。生成内容的保存与呈现遵循 [PROTOCOL](docs/PROTOCOL.md)，不擅自替换其表达语义。

拖拽、碰撞等机械反馈由程序实现；预制语音、本地反馈和兜底台词仅用于 [DESIGN](docs/DESIGN.md) 约定的直接交互、仪式行走和故障降级，不冒充实时推理。媒体及系统提醒不使用伙伴第一人称口吻。

## 代码验证规范

1. 按风险选择验证。缺陷修复先建立复现或可核查的定位依据；重构核对行为是否保持；逻辑和契约变更覆盖相关成功、失败与边界路径。文档变更核对事实、链接及差异。
2. 检查命令与前提统一见 [scripts/README.md](scripts/README.md)。提交前执行其中规定的完整检查入口，单独格式化不能代替门禁；自动修正后检查差异并复验，不通过关闭规则或降低标准获取通过结果。
3. 不向仓库提交测试代码。临时验证代码完成后清理，不误删既有仓库检查脚本；必要的脱敏复现输入、判定依据及关联版本和结果可以保留，便于复验。
4. 修复本次发现的问题后重跑受影响检查；已通过且未受后续改动影响的检查不机械重复。发现既有无关失败时说明依据和影响，不扩大修改范围或冒称全部通过。
5. 交付说明完成内容、实际执行的验证、结果与限制。静态检查、构建、模拟和真实运行的结论分别表述；无法执行的必要验证说明阻塞原因及剩余动作。

## Commit 消息规范

提交消息使用 [`.gitmessage`](.gitmessage) 模板。

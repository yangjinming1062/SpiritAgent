# RULES.md

项目级协作、代码、提示词、文档、提交与验证规范。具体产品行为、架构与平台支持按下文的事实归属维护。

## 协作规范

1. **按任务阅读**：阅读路由见 [AGENTS.md](AGENTS.md)。修改模块前理解其边界和相关约束；跨模块改动补读架构与协议，体验改动补读设计。拼写、链接等局部修正只需核对相关上下文。
2. **同步更新文档**：源码、结构或契约变化影响文档中的事实、约束或导航时，在同一提交同步对应的权威文档及引用；内部实现调整未改变这些内容时，无需修改文档。
3. **明确需求与授权边界**：先用现有文档、代码和用户指令消除疑问；仅当缺失信息会改变目标、公共契约、数据安全或不可逆操作且无法合理推断时询问，并说明原因。已授权范围内的可逆修改、检查及本次引入问题的修复连续完成，不逐步重复确认；非关键实现选择按项目惯例处理，影响结果的假设需说明。

## Commit 消息规范

按 [`.gitmessage`](.gitmessage) 模板写。

## 文档维护规范

### 读者与写作目标

根 [README.md](README.md) 是 GitHub 项目门面，面向人类介绍产品全貌、使用场景、形象演示与快速开始。其余工程文档面向 AI-Agent，帮助定位改动、理解设计原因、遵守约束及确认联动范围；不承担重复介绍产品的职责。

工程文档按以下原则维护：

1. 保留定位改动、理解设计、遵守约束和验证结果所需的信息：任务入口、设计意图、跨文件不变量、关键取舍、失败与恢复语义、已知限制。代码已直接且完整定义的清单、签名和字段映射链接源码；必要时保留最小示例解释语义，不复述逐函数调用链。
2. 一处事实一处维护：按下文的权威归属确定定义位置；其他文档可保留定位所需的简短概述并链接定义，不独立维护完整规则的副本。
3. 描述当前行为与约束，不以“以前 X、现在 Y”的变化叙事代替定义；仍影响部署或消费者的升级条件与兼容要求须保留。事实变化时按协作规范同步定义与引用。
4. 保留可搜索的标题、准确术语、条件、例外与失败后果。删重复背景和修辞，不以删除关键约束换短篇幅；摘要不能替代唯一的事实定义。
5. 默认用短段落或单层列表。只有多对象、多维度对照才用表格；顺序流程用编号列表或单行箭头。不要装饰性分隔线、ASCII 方框、重复目录树、整段加粗或为凑模板扩写。命令块保留可执行内容，协议表保留消歧义所需标识符。
6. 调整标题、搬移内容后检查相对路径、锚点及文字章节号；不复制旧段落维持导航。
7. Agent 入口只保留适用条件、项目特有约束与阅读路由；详细流程按任务进入相关文档。新增或强化指令须明确适用条件及要避免的实际误判；硬约束明确表达，写作偏好与实现建议保留判断空间，不因某个模型或一次失败叠加全局强制步骤。审查记录须标明基准和复核状态，不作为当前规范或未修复问题清单。

自检：这段是否帮助定位任务、理解原因、遵守约束或验证结果？有用则保留充分说明；重复则合并或链接定义，无助于这些目标则删除。

### 文档用语

散文使用中文，一个概念一种写法：供应商、客户端、后端、伙伴、角色定义、半身像、文生3D / 图生3D、打扰档位、变形目标。Runner、Installer 与 LLM、URL、GLB、WebSocket、JSON-RPC、Cron、token、schema、JWT、IPC、REST、venv、wheel、onboarding 等标准术语保留。

代码标识符只在消歧义、检索定位、命令、配置或契约定义需要时出现，并用反引号标记；不要把散文写成调用链。DESIGN 只使用产品语言，不出现字段、接口、配置键或供应商实现。

### 文档职责与事实归属

| 文档 | 维护职责 |
|---|---|
| [README.md](README.md) | 面向人类的产品介绍、演示、使用入口与快速开始；技术原理链接工程文档 |
| [AGENTS.md](AGENTS.md) | Agent 阅读路由；其他 Agent 入口链接它，不复制规范 |
| [RULES.md](RULES.md) | 协作、代码、提示词、文档、提交与验证规范 |
| [ARCHITECTURE.md](docs/ARCHITECTURE.md) | 模块边界、依赖与信任域、跨模块不变量、项目级技术取舍和平台支持 |
| [DESIGN.md](docs/DESIGN.md) | 产品体验与设计理由：形象、生命周期、交互、陪伴、语音和故障态 |
| [PROTOCOL.md](docs/PROTOCOL.md) | 跨模块通信、状态、事件、安全与凭据契约；明确变更需同步的参与方，避免复制完整代码 schema |
| [PIPELINE.md](docs/PIPELINE.md) | 3D / 2D 生成链、供应商能力、种子图、产物、恢复与客户端兑现契约 |
| 模块 README | 模块职责与包边界、任务导航、设计理由、内部约束、契约联动及操作与验证入口；子包 README 展开其独有内容 |
| [scripts/README.md](scripts/README.md) | 构建、发布、仓库级检查与调试操作；专项脚本 README 展开各自用法 |

跨 `backend/`、`client/`、`runner/`、`installer/` 主模块共同遵守的规则归 docs 中对应主题；同一主模块内多个子包共享的约束归最近的共同父级模块 README。外部依赖在本模块的用法与局部实现限制可写入本模块 README，要求其他主模块共同遵守的交互与交付规则须写入对应契约文档。

代码、schema、配置与检查脚本承载实际执行的结构、枚举、默认值和机器检查规则；文档维护这些定义的行为语义、设计理由与联动要求，并链接定义入口。代码与文档不一致时，结合用户要求、已约定的行为和实际消费链核实并修正；不能仅为匹配当前实现而改写约定，也不能因文档标为权威就忽略实现证据。

安装 payload 中的 SKILL.md、第三方参考与许可证按各自格式维护，不套用工程 README 规范。根 README 可概述用户可见能力，具体规则与阈值仍按上述归属维护。

### 子模块 README 规范

主模块及需要独立说明约束的子包 README 是按任务进入模块的维护入口。内容按实际主题组织，使用可检索标题；下列是信息要求，不是固定章节模板。章节可合并或省略，不强制六节、编号或特定标题用词，不按行数、句数、条目数或图表数量凑篇幅。

1. **定位与导航**：简要说明职责与边界，用必要的目录或源码入口定位包边界和依赖方向；按任务链接相关章节、子包或权威文档，不重复全局职责矩阵。
2. **设计与约束**：保留不了解就容易改错的非显然决策与不变量，解释决定及理由，按需要说明适用条件、例外和失败后果；仅在确有取舍时说明替代方案，不编造被否定的方案，也不把实现流水账写成设计决策。
3. **分层维护**：父 README 保留共享约束与导航，子包独有细节在相应 README 展开。多个可独立维护的主题按任务提供阅读路由，长篇细节按模块边界下沉；仅在存在独立维护内容时新建 README，不要求每个目录都有文档。
4. **契约与联动**：列出相关参与方、权威定义链接与变更需同步的内容；本模块的局部实现约束就地说明，共同遵守的契约按事实归属维护。链接尽量指向对应主题锚点或源码入口，不转抄完整 schema、枚举和默认值。
5. **限制与验证**：记录已确认且影响维护的限制，说明触发条件、影响与必要处置；没有独有限制时省略，不填“无”或“TODO / 待补”。操作与验证入口说明适用任务及必要前提，仓库级构建、发布与检查链接 scripts，本模块独有的操作可就地说明。

## 提示词设计与修改规范

适用于项目内的模型指令设计、修改与故障修复。本节维护通用原则，具体行为与输入输出契约按文档事实归属维护。不要求使用固定模板，修改与验证范围应与任务及风险相称。

1. **明确任务与成功标准。** 依据产品设计和模块契约，明确模型承担的职责、输入用途、预期结果及消费方式。区分必须满足的约束、默认行为与自主判断空间；对影响结果的抽象要求给出必要的判断依据，不以“精准”“自然”“高质量”等形容词代替任务定义。

2. **对齐实际输入与权限边界。** 以当前调用实际可见的消息、工具定义和多模态资料为准，区分指令与资料，说明必要的来源用途和冲突处理规则。不得假定模型已读取未提供或尚未取得的内容；引用资料和工具返回不能自行扩大授权。输入形态改变时，同步调整指代与适用要求。

3. **只保留对任务有效的信息。** 优先说明期望结果、需保留的特征和允许变化的范围，按实际风险补充限制、例外或示例。删除无关背景、重复强调和失效要求，消除规则冲突，但不以精简为由遗漏必要信息。共享约束集中维护，并按职责装配到每次调用；不把特定场景或模型的写法规定为统一模板。

4. **明确模型与代码的职责。** 模型承担语义理解、生成和推理判断；权限、状态、资源预算及可确定执行的校验由代码保障。影响模型决策的约束仍须传达，输出要求应与接口能力和消费端契约一致。缺失、冲突、失败与不可判断的情况按场景契约处理，不将推测或未完成操作表述为已确认事实。

5. **依据根因确定修改范围。** 结合实际输入、装配后的请求、原始返回和下游处理，区分提示词问题、资料问题、实现缺陷与能力限制。明确本次要改善的行为和必须保留的行为，只修改相关内容，不靠不断追加禁令掩盖根因。修改后复核完整请求的一致性；整体复核不等于整体重写。

6. **按风险验证实际效果。** 使用代表性输入、相关失败样例及原本有效的边界情况，对照修改前后的表现，并检查结果能否被消费端正确使用。验证条件尽量保持可比，留存复验所需的脱敏样例与判定依据。交付时说明验证范围、结果和限制；静态检查或模拟验证不能证明真实生成质量改善，单次成功不能证明稳定改善。

## 代码规范

以下七条原则是项目级约束，具体执行深度按改动风险确定。

### 原则一：编码前思考

确定目标、约束与影响范围后动手。区分可查证的事实、可自行决定的实现细节和需要用户决定的需求；澄清边界遵循协作规范。发现更简单的方案或实质性取舍时说明理由，不为列选项制造无关方案。

### 原则二：简洁优先——代码是负债，不是资产

**用最少的代码解决问题。不要过度推测。**

以清晰、正确和维护成本判断简洁，不以行数判断。没有实际需求的抽象、参数、开关与分支不添加。

- **只做被要求的事。** 不添加要求之外的功能，不塞未要求的"灵活性"或"可配置性"。
- **表达式与抽象以可读性为准。** 避免重复计算与无收益的透传包装；具备模块边界、公共契约或资源管理职责的入口不能仅因实现短而内联。
- **注释只解释代码推不出的约束。** 保留隐藏约束、非平凡返回语义与副作用的充分说明，不复述方法、字段或调用过程。设计决策与已知限制写模块 README，注释不替代文档。
- **按实际职责提取函数。** 为职责分解、资源管理或表达复杂逻辑可提取函数，不以调用次数作为唯一依据；共享抽象须有已发生的重复需求或明确的公共契约，不为假想复用增加通用层。
- **不为不可能的场景写错误处理。** 只处理真实会发生的失败；臆想分支是重量。
- **依赖是负债。** 新增依赖前先问：标准库或已有依赖能否解决？一个依赖就是一份升级、安全与兼容成本。落地：锁文件必须提交并显式锁定版本；依赖可被审计和升级；不为一个函数引入整个库。

### 原则三：精准修改

**只碰必须碰的。只清理自己造成的混乱。**

改现有代码时，每一行修改都要能直接追溯到这次请求。顺手"改进"相邻代码、注释或格式，就是在扩大影响半径、制造无关 diff。

- **不改没坏的东西。** 不重构、不重排、不"改进"与本次请求无关的代码、注释或格式。
- **匹配现有风格。** 即使你更喜欢另一种写法，也跟周围代码保持一致。
- **只清理本次产生的死代码。** 因你的改动而变得无用的导入、变量、函数立刻删掉。
- **无关的死代码先指出、不夹带删除。** 注意到预先存在且与本次无关的死代码，提出来；确认不可达后的清理按原则六另做，不塞进这次 diff。

**检验标准：每一行修改都能直接追溯到这次请求。**

### 原则四：目标驱动执行——解决问题 ≠ 完成任务

完成意味着请求范围内的实现、受影响的消费端与文档同步完成，相关验证通过，已发现的本次回归得到修复；不能在首版实现后留下必要验证就交付。受环境或授权阻塞时，说明已完成内容、阻塞证据与剩余动作。

- **先确定可验证目标。** 修 bug 先建立复现依据；重构用相关检查确认行为不变。复杂任务说明简短计划与验证点，局部修改直接完成。
- **验证与风险相称。** 文档修改检查内容一致性、路径、锚点与 diff；逻辑、协议及界面改动覆盖受影响的行为与失败路径。需要临时测试时使用一次性验证，完成后删除，不提交测试代码；项目中不包含测试代码。仓库检查入口见 [scripts/README.md](scripts/README.md)。
- **失败驱动复验。** 修复本次发现的问题后重跑受影响检查；通过后，只有新修改、失败或尚未消除的风险才扩大或重复验证，不为满足循环次数制造修改。交付说明验证结果与实际限制。
- **显式标注类型。** 参数与返回值都显式标注类型；结构化数据用语言原生的强类型定义表达，不用宽泛容器传递。宽泛类型只在对接外部不可控输入的边界短暂使用。类型是提前发现 BUG 的机制。
- **异常只在系统边界捕获。** 只在用户输入、外部服务、文件读写等边界处捕获异常并转成业务错误；内部逻辑用提前返回快速失败，不用大段捕获来掩盖问题——掩盖就是引入 BUG。
- **资源获取后必须保证释放。** 文件、锁、连接、定时器、事件监听、流等在退出前必须释放，用语言惯用的资源管理方式获取即初始化，不手动获取后靠末尾清理勉强关闭。
- **追求更高效的写法。** 大序列用惰性迭代，不一次性收集到内存；能用语言内建的聚合/映射能力就不手写逐元素循环，数值密集操作用批量/向量化库一次完成。

### 原则五：模块独立，逻辑解耦

每个模块、每条业务线、每条逻辑链在边界上尽量自洽，彼此只通过明确的公共入口通信。解耦的目标是让"改一处"的影响半径最小——这样才不会改一个地方牵出别处的新 BUG。重复是债，过度抽象也是债。

- **只经公共入口通信。** 跨模块调用必须走目标模块的公共入口，不越级深挖内部文件。
- **入口显式暴露对外契约。** 只有公共入口才需要显式列出对外符号；纯内部注册、无对外符号的入口保持空白，不为导出而设空导出列表。
- **保持目录入口干净。** 入口目录只放入口、配置和数据模型；业务逻辑下沉到领域模块。
- **全局量集中在文件头部。** 模块级常量、全局变量在导入声明区下方集中定义，不散落在文件中部或函数之间。
- **共享代码就近维护。** 共享函数与常量归能覆盖实际调用者的最小公共模块，通过该模块的公共入口使用；不把无关能力塞进通用工具模块。
- **提交前先格式化。** 提交前执行 `uv tool run pre-commit run -a`（不要绕过它直接运行单个格式化工具，配置会不一致）。

### 原则六：死代码和历史包袱是炸药

没被调用的代码不是资产，是会让后来者误判的炸药——误以为某条路径还活着、某个兼容入口还需要维护，从而做出错误决策。确认不可达就删，不做"先留着以后有用"。

- **立刻删除死代码。** 无人调用的兼容垫片、零活跃调用者的别名、为不存在的循环依赖风险写的防御性重复导入、永远返回同一值的分支，确认不可达后删掉。
- **与本次无关的死代码报告位置与依据。** 另有清理任务时再删除，不扩大当前请求范围（见原则三）。
- **内部重命名同步调用者。** 可在同一变更中同步的内部名称直接重命名并更新所有调用者，不用别名省略修改。公共接口、持久化键或跨版本消费者确需兼容时，明确迁移与移除条件；跨模块变更遵循 [PROTOCOL 的维护规约](docs/PROTOCOL.md#8-维护规约)，不无依据保留兼容层。

### 原则七：伙伴相关内容由大模型动态给出

面向用户的伙伴表达由大模型结合情境、记忆与即时互动推理产生，机械响应及已约定的降级按下述边界处理。

- **不写程序化伙伴文案。** 禁止用时间段、状态枚举或静态模板伪造心情、心理活动、口头反应、主动表达等角色内容。
- **按交付契约保留表达。** 角色内容的持久化、下发与呈现遵循 [PROTOCOL](docs/PROTOCOL.md#14-聊天心情视觉表达与空间契约)；保留应交付内容的表达语义，不改写成静态模板。正文、元数据的分离及中间轮处理按该契约执行。
- **缺失时遵循对应降级边界。** 尚未收到动态内容且不属于已约定的降级场景时，只展示真实角色定义或客观状态，不填塞假造的伙伴语句。
- **机械响应除外。** 拖拽跟随、碰撞回弹等物理或操作反馈由程序兑现，不走大模型。
- **离线与机械降级不冒充实时推理。** 允许的本地反馈、预制语音及兜底台词按 [DESIGN 的直接交互约定](docs/DESIGN.md#63-用户直接交互戳拖悬停)、[仪式行走约定](docs/DESIGN.md#36-仪式性行走ritual-walk) 和[故障态约定](docs/DESIGN.md#65-故障态与降级行为伙伴永不死) 使用；媒体与系统提醒使用非人格化通知，不以伙伴第一人称口吻呈现。

# RULES.md

项目规范（协作、代码、文档、Commit、平台支持策略）。

## 协作规范

1. **阅读文档优先**：优先阅读[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)和[docs/DESIGN.md](docs/DESIGN.md)了解项目思路，处理具体模块前**先读**该模块 `README.md`。
2. **同步更新文档**：修改源码/结构/导出/架构后，**同一提交**里同步该模块及所有受影响的文档。
3. **明确需求**：不懂的要及时问（说清楚为什么提这个问题），直到对目标有明确认知后再开始干活。

## Commit 消息规范

按 [`.gitmessage`](.gitmessage) 模板写。

## 文档维护规范

1. **同步更新**：改实现方案/架构设计 → 同提交同步该模块及**所有受影响**的 README.md。
2. **只记读代码推不出的东西**：设计意图（为什么这样选）、跨文件契约（不变量、约束）、决策权衡、已知限制。代码本身的结构——文件树逐文件职责、类的方法列表、函数级数据流、类型签名、JSONB 列映射——读代码或 spec 文档即可得出，不写。
3. **描述当前状态，不写变化叙事**：不写"X 不再 Y"、"现在改为 Z"、"从 W 移到了 V"。直接陈述当前架构（"X 在 Y 处调用"）。git log / blame 负责历史。
4. **跨模块共享的设计放共同祖先**：多个子模块共用的设计决策，记在最近的共同祖先 README 中，不在每个子模块重复。子模块 README 只记该模块**独有**的决策。
5. **一处事实一处维护**：每条可复用事实必须先在文档职责矩阵中找到唯一 owner；其他文档只允许导航链接，不复述事实、不保留同义转写。新增内容前先搜索既有文档；若职责变化，同步移动整段内容并更新所有链接。

**写完一段后自检：删掉这段，读者会不会误解架构、违反约束、或做出错误决策？不会 → 这段是代码描述，删掉。**

| 类别 | 写 | 不写 |
|------|----|----|
| 设计意图 | 为什么这样选、有哪些权衡 | — |
| 行为契约 | 协议级不变量、关键约束 | 类名/方法签名/字段类型 |
| 业务规则 | 跨模块数据契约、状态转换规则 | 逐字段列映射 |
| 安全/隐私 | API Key fingerprinting、路径白名单 | 具体 schema |
| 已知限制 | 已知坑、临时绕过方案 | 注释里已写的 TODO |
| 架构地图 | 层/包的边界与依赖方向（≤5 行） | 逐文件列职责/导出列表 |
| 数据流 | 层/模块之间的数据流（架构级） | 逐函数调用链 |
| spec 内容 | — | ✓ 留给 spec 文档 |
| 代码块 | — | ✓ 留给源文件 |

### 文档用语

**散文中文化，一个概念一种写法。** 组件英文名只出现在拓扑图、契约表、代码引用里：

| 概念 | 统一写法 | 不出现在散文 |
|------|----------|--------------|
| LLM/生图/3D 服务方 | 供应商 | provider |
| 桌面应用 | 客户端 | Client |
| 云端服务 | 后端（进程细分写"后端 web 进程"） | Backend |
| 渲染工作进程 | 渲染 Worker | render worker / worker |
| 产品主体 / 角色数据 | 伙伴 / 角色定义 | 桌宠 / persona |
| 2D 写实肖像 | 半身像 | portrait |
| 文生/图生模型 | 文生3D / 图生3D | text-to-3d |
| 免打扰等级 | 打扰档位 | disturbance tier |
| 面部表情变形（眨眼/口型） | 变形目标 | morph（注入/表情/target）、blendshape |

行业标准词保留英文:LLM、URL、GLB、WebSocket、JSON-RPC、Cron、token、schema、JWT、IPC、WS、REST、venv、wheel、onboarding。Runner、Installer 作为模块专名保留。

**散文不写代码标识符**（函数/类/常量/字段/状态名，含反引号包裹）。标识符只出现在其职责处：契约表中的方法与事件名、架构地图文件树、命令块、配置键与路径引用。散文用自然语言指代——写"供应商任务 id 与下载 URL 持久化进模型行"，不写字段名。

### 顶层文档职责（文档分层）

四个顶层文档各司其职，**同一事实只写一处**、其余文档链接引用，禁止复制粘贴。写文档前先判断内容属于哪一层：

| 文档 | 职责 | 禁止 |
|------|------|------|
| [README.md](README.md) | 产品定位 → 核心功能 → 快速开始 | 不写架构机制、安全契约、具体代码逻辑 |
| [ARCHITECTURE.md](docs/ARCHITECTURE.md) | 项目级架构与技术方向（指导类）：模块边界与为何分离、跨模块不变量、重大技术取舍 | 不写具体代码（函数名/配置键/逐字段）、不写产品功能设计、不写协议枚举明细 |
| [DESIGN.md](docs/DESIGN.md) | 产品功能设计与思考：形象、动画、生命周期、onboarding、陪伴范式、语音、故障态 | **无任何代码相关内容**——字段名、RPC 方法名、配置键、供应商名、类型签名一律不出现（归 PROTOCOL 或模块 README） |
| [PROTOCOL.md](docs/PROTOCOL.md) | 跨模块契约的**思想层**描述：有哪些契约、改一处需同步哪些模块（防只改一处导致遗漏） | 不复述代码（config schema 表、JSON 示例、逐参数方法签名）、不写实现细节 |

专项事实只由专项文档展开：

| 专项 | 权威文档 |
|------|----------|
| 3D 生成链路、供应商能力、种子图编排、产物与客户端兑现 | [docs/PIPELINE.md](docs/PIPELINE.md) |
| 构建、发布、导入检查与仓库级脚本 | [scripts/README.md](scripts/README.md) |

顶层文档与模块 README 只链接专项事实，不复制其规则、阈值或流程。

**通用原则**：
- ARCHITECTURE / DESIGN / README 三者均为**描述性质**，不描述具体代码逻辑。
- 判断"权威归属"再写：这份事实的权威定义属于哪个文档，就只在那里展开，其余文档链接引用。判断标准是"改这个事实时，开发者必须去改哪个文档"。
- 模块 `README.md` 记录**读代码读不到的**：设计意图（为什么这样选）、跨文件不变量、决策权衡、已知限制；可直接读代码得出的（文件树逐文件职责、函数级数据流、类型签名、字段映射）不写。

### 子模块 README 规范

`backend/`、`client/`、`runner/`、`installer/` 各自的 `README.md` 是**模块内开发者的入口文档**，目标读者是"要在该模块内改代码或审查代码的人"。它们必须按下面 6 节固定结构组织，缺哪一节就显式写"无"或合并到上一节，**不能**跳过。

文档分层原则：
- 架构动机、不变量 → [ARCHITECTURE.md](docs/ARCHITECTURE.md)
- 跨模块契约（JSON-RPC、枚举、事件、安全、凭据）→ [PROTOCOL.md](docs/PROTOCOL.md)
- 产品设计意图 → [DESIGN.md](docs/DESIGN.md)
- **模块实现、文件树、配置项、错误码、本模块独有的设计决策 → 该模块 `README.md`**

#### §1 职责与边界（3–8 行）

这个模块是什么、**不是**什么。一句话定位 + 显式排除（它**不**做哪些事）。链接 [ARCHITECTURE.md](docs/ARCHITECTURE.md) 对应锚定读者该先读架构层定位。

**不**放：架构图（已在根 docs/ARCHITECTURE.md）、依赖方向、文件树。

#### §2 设计意图（核心节，8–20 行）

本模块为什么这样设计——模块级的设计哲学/原则，**只**写"读代码推不出的"取舍。列举 3–N 条设计原则（每条 1–3 行，说"原则 + 为什么这样 + 不这样会怎样"）。

典型例子："Runner 零凭证"、"Backend 启动时自动执行 Alembic 迁移的设计选择"、"Installer binary self-contained"。

**不**放：具体类名/方法名（实现细节）、版本号/数字阈值（可调参数）。

#### §3 架构地图（1 个图 + 5–10 行）

目录结构 + 依赖方向：用树形结构画模块顶层目录（≤ 3 层深），明确依赖方向箭头。只描述**包边界和依赖方向**，**不**逐文件列职责。

**不**放：文件级职责列表、类导出列表、import 顺序规则（那些走本文 §代码规范）。

#### §4 关键设计决策（每条 3–5 行，无数量上限但每条都要"非显然"）

列出"会改变读者对模块理解的关键决策"——读者改了某个东西如果不读这节，会踩坑。每条格式：**决定 + 为什么 + 替代方案被否定的理由**。

典型例子："Backend provider 自注册而非手动 `main.py` 引入"、"Client safeStorage 跨平台一致（DPAPI/Keychain/libsecret）"、"Runner 全部 RPC 经 WS 而非 stdio"、"Installer 用 uv-managed venv 而非 system Python"。

**不**放：已写在 §2 设计意图里的（避免重复）、trivial 的（"为什么用 TypeScript"）。

#### §5 与外部的契约（5–15 行）

本模块对外承诺的契约 + 依赖的外部契约。**本模块独有的**写在这里，跨模块共有的**链**到 [PROTOCOL.md](docs/PROTOCOL.md) / [ARCHITECTURE.md](docs/ARCHITECTURE.md)。表格形式：**契约名 + 方向 + 在哪定义**（`PROTOCOL.md §X` / `本模块独有` / `ARCHITECTURE.md §X`）。

#### §6 已知限制

只列**模块内部决定**的限制（不是已写在 [ARCHITECTURE.md §10](docs/ARCHITECTURE.md) 全局不变量的）。表格格式：**限制 + 说明**。

#### 子模块 README 反模式（不要出现）

- 逐文件列职责、逐函数列调用链、provider 协议对比表（代码 review 时该看的）
- 复述 [PROTOCOL.md](docs/PROTOCOL.md) / [ARCHITECTURE.md](docs/ARCHITECTURE.md) / [DESIGN.md](docs/DESIGN.md) 已有的契约（链过去即可）
- 列版本号、字段长度、配置项默认值（那些是代码）
- "以前 X，现在 Y"的叙事（git log 负责）
- "TODO" / "待补"占位段（要么删要么补，不留空的）

子模块 README 的"质量门槛"自检：删掉每一段，读者会不会误解模块设计、违反约束、做出错误决策？不会 → 这段是代码描述，删掉。

## 代码规范

> 项目级强制约束。**先读懂下面七条原则——它们是判断一切代码写法的依据；后面的具体规则只是这些原则在常见场景下的落地，不是原则本身。** 这些原则偏向谨慎而非速度；拼写修正、显而易见的一行修改自行判断，不必走完整推敲。

### 原则一：编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

动手前先把目标和约束摊开。默默选一种解释然后执行，就是在用错误假设换返工。不确定就问，有歧义就并列呈现，有更简单的路径就提出异议。

- **明确说明假设。** 不确定时询问，不猜测后直接执行。
- **歧义先摊开。** 存在多种解释时并列写出，不默默选一种开干。
- **该反驳时反驳。** 有更简单的做法就说；需求会引入不必要复杂度时提出异议。
- **困惑时停下来。** 指出不清楚的地方并要求澄清，不带着困惑继续写。

### 原则二：简洁优先——代码是负债，不是资产

**用最少的代码解决问题。不要过度推测。**

每行代码都是一个潜在的 BUG 和一份长期维护成本。能用 10 行解决的问题不写 100 行；行数多不代表健壮，只代表攻击面更大。所有"以后可能用得上"的抽象、参数、开关、分支，在真实调用者出现之前都是重量，先不写。

- **只做被要求的事。** 不添加要求之外的功能，不塞未要求的"灵活性"或"可配置性"。
- **能用一行就不用两行。** 多行表达式能合并成一行且读起来同样清晰就合并；简单的条件判断写成条件表达式；只用到一次的临时值就地绑定，不重复计算。
- **只作一层透传的包装函数要内联。** 一个函数只是对另一个函数的一行透传且只有一处调用，就内联掉、直接调用底层函数，不增加没有收益的间接层。
- **默认不写注释，只写从代码本身推不出的约束。** 文档注释同样适用——只解释不显然的东西（隐藏约束、非平凡返回语义、副作用），最多一行短句；既不消歧义、也不属于公共契约的类型说明直接去掉。
  - **删掉会不会让读者困惑？** 如果只是为了省去"读一行代码"的事，删掉。
  - **是"为什么"还是"做什么"？** 复述代码"做什么"（路由、请求方法、字段约束、类型签名、函数名）就删掉。
  - **保持精简。** 超过约 3 行通常意味着该拆成更小的函数或改进命名。
  - **注释不能替代文档。** 设计决策与已知限制写模块 README；新手看不懂先修命名和结构，注释是最后手段。
- **不为未发生的需求预留抽象。** 同样的模式在 2+ 个同级调用者出现、同一常量在多个模块重复定义、或行数开始咬人时，才抽共享函数；第一次出现不抽。
- **不为不可能的场景写错误处理。** 只处理真实会发生的失败；臆想分支是重量。
- **能写短就写短。** 200 行能写成 50 行就重写。写完问：资深工程师会觉得这过于复杂吗？会 → 简化。
- **依赖是负债。** 新增依赖前先问：标准库或已有依赖能否解决？一个依赖就是一份升级、安全与兼容成本。落地：锁文件必须提交并显式锁定版本；依赖可被审计和升级；不为一个函数引入整个库。

### 原则三：精准修改

**只碰必须碰的。只清理自己造成的混乱。**

改现有代码时，每一行修改都要能直接追溯到这次请求。顺手"改进"相邻代码、注释或格式，就是在扩大影响半径、制造无关 diff。

- **不改没坏的东西。** 不重构、不重排、不"改进"与本次请求无关的代码、注释或格式。
- **匹配现有风格。** 即使你更喜欢另一种写法，也跟周围代码保持一致。
- **只清理本次产生的死代码。** 因你的改动而变得无用的导入、变量、函数立刻删掉。
- **无关的死代码先指出、不夹带删除。** 注意到预先存在且与本次无关的死代码，提出来；确认不可达后的清理按原则六另做，不塞进这次 diff。

**检验标准：每一行修改都能直接追溯到这次请求。**

### 原则四：目标驱动执行——解决问题 ≠ 完成任务

**定义成功标准。循环验证直到达成。**

把指令式任务转成可验证的目标，再循环执行到达标。弱标准（"让它工作"）会逼着不断澄清；强标准才能独立做完。解决问题不等于完成任务——每次修改先解决眼前的问题，然后至少追问两句：**这样改会不会引入新的 BUG？有没有更高效的解法？** 改一处时先想清楚它的影响半径——不要用"修好这个"换来"搞坏那个"。局部写法服务全局：优先选让整体更清晰、关键路径更高效的方案。

- **先把任务转成可验证目标。** "加校验"→ 列出无效输入的验收条件，用一次性测试验证后再让实现通过；"修 bug"→ 先复现再修到复现失败；"重构"→ 重构前后用同一组检查确认行为不变。
- **多步骤任务写出短计划。** 每一步带验证点（步骤 → 验证: 检查），按计划循环直到全部达标。
- **用一次性测试验证，不留测试代码。** 开发时写测试验证本次逻辑正确；达标即删，不提交仓库。项目中不包含测试代码。
- **交付前自检。** 验证眼前问题被解决之外，还要确认没有引入新 BUG，并比较过是否存在更高效的解法；确认过才算完成。
- **显式标注类型。** 参数与返回值都显式标注类型；结构化数据用语言原生的强类型定义表达，不用宽泛容器传递。宽泛类型只在对接外部不可控输入的边界短暂使用。类型是提前发现 BUG 的机制。
- **异常只在系统边界捕获。** 只在用户输入、外部服务、文件读写等边界处捕获异常并转成业务错误；内部逻辑用提前返回快速失败，不用大段捕获来掩盖问题——掩盖就是引入 BUG。
- **资源获取后必须保证释放。** 文件、锁、连接、定时器、事件监听、流等在退出前必须释放，用语言惯用的资源管理方式获取即初始化，不手动获取后靠末尾清理勉强关闭。
- **追求更高效的写法。** 大序列用惰性迭代，不一次性收集到内存；能用语言内建的聚合/映射能力就不手写逐元素循环，数值密集操作用批量/向量化库一次完成。

### 原则五：模块独立，逻辑解耦

每个模块、每条业务线、每条逻辑链在边界上尽量自洽，彼此只通过明确的公共入口通信。解耦的目标是让"改一处"的影响半径最小——这样才不会改一个地方牵出别处的新 BUG。重复是债，过度抽象也是债。

- **只经公共入口通信。** 跨模块调用必须走目标模块的公共入口，不越级深挖内部文件。
- **入口显式暴露对外契约。** 只有公共入口才需要显式列出对外符号；纯内部注册、无对外符号的入口保持空白，不为导出而设空导出列表。
- **保持目录入口干净。** 入口目录只放入口、配置和数据模型；业务逻辑下沉到领域模块。
- **全局量集中在文件头部。** 模块级常量、全局变量在导入声明区下方集中定义，不散落在文件中部或函数之间。
- **共享函数放在最小共同调用者旁。** 当重复真实出现时才抽共享函数，放在最小的调用者旁边，不塞进通用工具模块。
- **提交前先格式化。** 提交前执行 `uv tool run pre-commit run -a`（不要绕过它直接运行单个格式化工具，配置会不一致）。

### 原则六：死代码和历史包袱是炸药

没被调用的代码不是资产，是会让后来者误判的炸药——误以为某条路径还活着、某个兼容入口还需要维护，从而做出错误决策。确认不可达就删，不做"先留着以后有用"。

- **立刻删除死代码。** 无人调用的兼容垫片、零活跃调用者的别名、为不存在的循环依赖风险写的防御性重复导入、永远返回同一值的分支，确认不可达后删掉。
- **与本次无关的死代码单独清理。** 确认不可达后仍要删，但不塞进无关请求的 diff（范围见原则三）。
- **不写向后兼容的别名。** 重构时直接使用原名并全量重命名，不为减少重命名而写别名。

### 原则七：伙伴相关内容由大模型动态给出

**除拖拽等机械响应外，凡面向用户的伙伴表达都由大模型结合情境、记忆与即时互动推理产生，不靠程序拼装。**

- **不写程序化伙伴文案。** 禁止用时间段、状态枚举或静态模板伪造心情、心理活动、口头反应、主动表达等角色内容。
- **推理结果原样送达呈现层。** 大模型给出的角色内容必须持久化并下发，不丢弃、不改写成模板。
- **缺失时只回退到真实人设。** 尚未收到动态内容时，只展示用户设定的性格等角色定义，不填塞假造语句。
- **机械响应除外。** 拖拽跟随、碰撞回弹等物理或操作反馈由程序兑现，不走大模型。

# RULES.md

项目规范（协作、代码、提示词、文档、Commit、平台支持策略）。

## 协作规范

1. **按任务阅读**：阅读路由见 [AGENTS.md](AGENTS.md)。修改模块前理解其边界和相关约束；跨模块改动补读架构与协议，体验改动补读设计。拼写、链接等局部修正只需核对相关上下文。
2. **同步更新文档**：修改源码/结构/导出/架构后，**同一提交**里同步该模块及所有受影响的文档。
3. **明确需求与授权边界**：先用现有文档、代码和用户指令消除疑问；仅当缺失信息会改变目标、公共契约、数据安全或不可逆操作且无法合理推断时询问，并说明原因。已授权范围内的可逆修改、检查及本次引入问题的修复连续完成，不逐步重复确认；非关键实现选择按项目惯例处理，影响结果的假设需说明。

## Commit 消息规范

按 [`.gitmessage`](.gitmessage) 模板写。

## 文档维护规范

### 读者与写作目标

根 [README.md](README.md) 是 GitHub 项目门面，面向人类介绍产品全貌、使用场景、形象演示与快速开始。其余工程文档面向 AI-Agent，帮助定位改动、理解设计原因、遵守约束及确认联动范围；不承担重复介绍产品的职责。

工程文档按以下原则维护：

1. 只保留会影响修改或审查的信息：设计意图、跨文件不变量、关键取舍、失败与恢复语义、已知限制。代码可直接读出的文件清单、方法签名、字段映射与逐函数调用链不复述；必要时链接源码入口。
2. 一处事实一处维护：先确定下表中的权威归属，再写入；其他文档链接到该处。跨模块事实归 docs 中对应主题，不上移到面向人类的根 README；模块内部共享事实归最近的模块 README。
3. 描述当前状态，不写“以前 X、现在 Y”。实现、结构或契约变化时，同一提交更新所有受影响的权威文档和引用。
4. 保留可搜索的标题、准确术语、条件、例外与失败后果。删重复背景和修辞，不以删除关键约束换短篇幅；摘要不能替代唯一的事实定义。
5. 默认用短段落或单层列表。只有多对象、多维度对照才用表格；顺序流程用编号列表或单行箭头。不要装饰性分隔线、ASCII 方框、重复目录树、整段加粗或为凑模板扩写。命令块保留可执行内容，协议表保留消歧义所需标识符。
6. 调整标题、搬移内容后检查相对路径、锚点及文字章节号；不复制旧段落维持导航。
7. Agent 入口只保留适用条件、项目特有约束与阅读路由；详细流程按任务进入相关文档。新增指令须能说明会避免哪类实际误判，不因某个模型或一次失败叠加全局强制步骤。审查记录须标明基准和复核状态，不作为当前规范或未修复问题清单。

自检：删掉这段，Agent 是否可能误解边界、违反约束或做出错误决策？不会则删；会则保留最短的充分说明。

### 文档用语

散文使用中文，一个概念一种写法：供应商、客户端、后端、伙伴、角色定义、半身像、文生3D / 图生3D、打扰档位、变形目标。Runner、Installer 与 LLM、URL、GLB、WebSocket、JSON-RPC、Cron、token、schema、JWT、IPC、REST、venv、wheel、onboarding 等标准术语保留。

代码标识符只在消歧义、检索定位、命令、配置或契约定义需要时出现，并用反引号标记；不要把散文写成调用链。DESIGN 只使用产品语言，不出现字段、接口、配置键或供应商实现。

### 文档职责与事实归属

| 文档 | 唯一职责 |
|---|---|
| [README.md](README.md) | 面向人类的产品介绍、演示、使用入口与快速开始；技术原理链接工程文档 |
| [AGENTS.md](AGENTS.md) | Agent 阅读路由；其他 Agent 入口链接它，不复制规范 |
| [RULES.md](RULES.md) | 协作、代码、提示词、文档、提交与验证规范 |
| [ARCHITECTURE.md](docs/ARCHITECTURE.md) | 模块边界、依赖与信任域、跨模块不变量、项目级技术取舍和平台支持 |
| [DESIGN.md](docs/DESIGN.md) | 产品体验与设计理由：形象、生命周期、交互、陪伴、语音和故障态 |
| [PROTOCOL.md](docs/PROTOCOL.md) | 跨模块通信、状态、事件、安全与凭据契约；明确变更需同步的参与方，避免复制完整代码 schema |
| [PIPELINE.md](docs/PIPELINE.md) | 3D / 2D 生成链、供应商能力、种子图、产物、恢复与客户端兑现契约 |
| 模块 README | 模块独有的设计意图、包边界、关键决策、外部契约导航、内部限制；子包 README 只展开该子包独有约束 |
| [scripts/README.md](scripts/README.md) | 构建、发布、仓库级检查与调试操作；专项脚本 README 展开各自用法 |
| [SECURITY.md](docs/SECURITY.md) | 漏洞报告与安全事件记录；事件历史作为审计证据保留 |

安装 payload 中的 SKILL.md、第三方参考与许可证按各自格式维护，不套用工程 README 模板。根 README 可概述用户可见能力，具体规则与阈值仍只在工程文档维护。

### 子模块 README 规范

`backend/`、`client/`、`runner/`、`installer/` 的 README 是修改模块前的 Agent 入口，保留以下六节便于稳定检索；不按行数、条目数或图表数量凑篇幅。子包按实际内容组织，不强套六节。操作说明确需本模块独有时可追加操作节，仓库级构建与检查仍归 scripts。

1. 职责与边界：一句定位、必要的排除项与架构链接；不重复模块开场白或全局职责矩阵。
2. 设计意图：模块级原则及理由；不提前展开 §4 的具体机制，也不复述全局不变量。
3. 架构地图：用少量路径定位包边界及依赖方向；优先短列表或依赖表达式，不画逐文件目录树。
4. 关键设计决策：只收录不了解就容易改错的非显然决策。每项以可检索主题开头，通常 1–3 句写清决定、理由与必要例外；仅在确有取舍时说明替代方案，不编造被否定的方案。长项按独立约束拆分；跨模块规则链接权威文档。标题保留“关键”，不能靠改名成“设计决策”把实现流水账纳入。
5. 与外部的契约：列参与方与定义链接；模块独有的外部约束在此定义，不复述 PROTOCOL / ARCHITECTURE / PIPELINE。
6. 已知限制：列触发条件、影响及必要处置；不重复前述决策。确无独有限制不写。

禁止：逐文件职责清单、逐参数签名、配置默认值转抄、重复契约、变化叙事、“TODO / 待补”占位，以及只有“用了什么”却不解释改动风险的决策。

## 提示词设计与修改规范

适用于系统提示词、工具说明、结构化推理、媒体生成与视觉校验等模型指令的设计、修改和故障修复。本节规定通用原则；场景契约按[文档职责与事实归属](#文档职责与事实归属)维护，具体措辞与接口适配落实在相应实现中。每次编辑都须从任务目标重新审视受影响提示词的完整内容与编排，以信息充分、表述准确、组织连贯和内容取舍有依据为标准。

1. **从设计意图定义任务。** 以项目设计和所属模块契约为依据，明确模型承担的职责、期望行为或产物、必要输入的用途，以及输出的使用方式和成功标准。区分必须满足的约束、可被本次要求调整的默认行为与允许自主判断的空间；存在信息缺失或冲突时，明确依据与处理原则。角色设定应服务于实际判断和表达，不以身份口号代替任务说明，也不把单一场景的文风或流程推广到所有任务。
2. **以清晰的目标和边界引导。** 优先说明要达到的结果、需保持的特征与判断依据；必要的否定约束、条件规则和示例可用于消除歧义，其适用范围须明确并与目标一致。示例用于解释要求，避免让偶然细节成为隐含规则。约束的细致程度以任务需要为准：有顺序依赖或固定交付契约时明确规定，其余部分保留情境判断与表达自由，不靠冗长清单、反复强调或固定话术替代清楚的要求。
3. **区分指令权限与资料用途。** 指令、用户本次要求、背景资料和反馈应可辨识，明确各自来源、作用范围及冲突优先级；规则权限不能仅由内容是否稳定或采用何种格式决定。运行资料在任务授予的范围内影响结果，其中的文字不能自行取得更高指令权限或扩大授权。指令与数据的组织形式按接口能力和可读性选择；即使共用一个字段，也须表达清楚边界。摘要、裁切与转述应保留当前任务必需的意图、约束及依据，不能悄然改变任务。
4. **推敲内容取舍与整体编排。** 逐项审视已有与拟新增内容对当前任务的作用，决定保留、补充、删除、合并或改写；每项取舍都应有任务依据。补足影响理解或执行的缺失信息，精简时核对必要条件、例外和判断依据是否仍然完整；清理重复、无关或失效内容，不以篇幅长短衡量质量。按语义依赖安排段落，使目标、依据、约束和交付要求之间的关系清楚。共享要求集中维护，各块按职责提供内容；以一次调用实际可见的完整输入复核信息是否充分、表述是否一致，不能假定模型知道未传入的规则或资料。
5. **让模型与代码各负其责。** 模型承担语义理解、内容生成和需要推理的判断；可确定计算的规则由代码校验或执行，权限、状态流转和资源预算不能仅靠提示词保障。影响模型选择或生成结果的约束仍须明确传达，代码校验不能替代任务说明。模型被要求完成的行为及其验收标准，应与可用输入、工具、供应商能力和消费端契约一致；格式合规或模型自评通过不等于任务成功。
6. **依据根因与整体审视决定修改。** 修改前理解实际调用与消费链；排障时从输入、装配后的请求、原始返回和下游处理定位原因，区分指令问题、资料问题、代码缺陷与能力限制。修改范围由整体审视结果决定：缺失或含混的要求在其职责位置澄清，目标、结构或约束关系有问题时重组相关描述，并检查关联提示词。局部修改也须与完整任务一致；仅追加限制或让当前失败样例通过不足以完成修改。重试保留仍有效的原任务与约束，反馈明确本次修正目标；临时反馈不自动成为长期规则，不得为通过校验擅自放宽任务要求。
7. **以可比较的结果验证。** 核对实际发送的指令与资料，按改动风险覆盖代表性正常场景、相关失败样例和原本有效的边界情况；涉及多语言、参考输入、工具或供应商适配时检查相应分支，并确认输出能被消费端正确使用。效果判断应在条件可比时对照修改前后的结果，考虑模型输出的波动，结论范围与证据相称。静态、装配和模拟验证只证明对应环节；声称生成质量或供应商行为改善须有真实调用证据。交付时简要说明主要内容取舍的依据、整体复核与验证结果，未验证的效果明确标注。临时验证与复验范围遵循[代码验证规范](#原则四目标驱动执行解决问题--完成任务)。

## 代码规范

以下七条原则是项目级约束，具体执行深度按改动风险确定。

### 原则一：编码前思考

确定目标、约束与影响范围后动手。区分可查证的事实、可自行决定的实现细节和需要用户决定的需求；澄清边界遵循协作规范。发现更简单的方案或实质性取舍时说明理由，不为列选项制造无关方案。

### 原则二：简洁优先——代码是负债，不是资产

**用最少的代码解决问题。不要过度推测。**

以清晰、正确和维护成本判断简洁，不以行数判断。没有实际需求的抽象、参数、开关与分支不添加。

- **只做被要求的事。** 不添加要求之外的功能，不塞未要求的"灵活性"或"可配置性"。
- **表达式与抽象以可读性为准。** 避免重复计算与无收益的透传包装；具备模块边界、公共契约或资源管理职责的入口不能仅因实现短而内联。
- **注释只解释代码推不出的约束。** 保留隐藏约束、非平凡返回语义与副作用的充分说明，不复述方法、字段或调用过程。设计决策与已知限制写模块 README，注释不替代文档。
- **不为未发生的需求预留抽象。** 同样的模式在 2+ 个同级调用者出现、同一常量在多个模块重复定义、或行数开始咬人时，才抽共享函数；第一次出现不抽。
- **不为不可能的场景写错误处理。** 只处理真实会发生的失败；臆想分支是重量。
- **依赖是负债。** 新增依赖前先问：标准库或已有依赖能否解决？一个依赖就是一份升级、安全与兼容成本。落地：锁文件必须提交并显式锁定版本；依赖可被审计和升级；不为一个函数引入整个库。

### 原则三：精准修改

**只碰必须碰的。只清理自己造成的混乱。**

改现有代码时，每一行修改都要能直接追溯到这次请求。顺手"改进"相邻代码、注释或格式，就是在扩大影响半径、制造无关 diff。

- **不改没坏的东西。** 不重构、不重排、不"改进"与本次请求无关的代码、注释或格式。
- **匹配现有风格。** 即使你更喜欢另一种写法，也跟周围代码保持一致。
- **只清理本次产生的死代码。** 因你的改动而变得无用的导入、变量、函数立刻删掉。
- **无关的死代码先指出、不夹带删除。** 注意到预先存在且与本次无关的死代码，提出来；确认不可达后的清理按原则六另做，不塞进这次 diff。

**检验标准：每一行修改都能直接追溯到这次请求。**

### 原则四：目标驱动执行——解决问题 ≠ 完成任务

完成意味着请求范围内的实现、受影响的消费端与文档同步完成，相关验证通过，已发现的本次回归得到修复；不能在首版实现后留下必要验证就交付。受环境或授权阻塞时，说明已完成内容、阻塞证据与剩余动作。

- **先确定可验证目标。** 修 bug 先建立复现依据；重构用相关检查确认行为不变。复杂任务说明简短计划与验证点，局部修改直接完成。
- **验证与风险相称。** 文档修改检查内容一致性、路径、锚点与 diff；逻辑、协议及界面改动覆盖受影响的行为与失败路径。需要临时测试时使用一次性验证，完成后删除，不提交测试代码；项目中不包含测试代码。仓库检查入口见 [scripts/README.md](scripts/README.md)。
- **失败驱动复验。** 修复本次发现的问题后重跑受影响检查；通过后，只有新修改、失败或尚未消除的风险才扩大或重复验证，不为满足循环次数制造修改。交付说明验证结果与实际限制。
- **显式标注类型。** 参数与返回值都显式标注类型；结构化数据用语言原生的强类型定义表达，不用宽泛容器传递。宽泛类型只在对接外部不可控输入的边界短暂使用。类型是提前发现 BUG 的机制。
- **异常只在系统边界捕获。** 只在用户输入、外部服务、文件读写等边界处捕获异常并转成业务错误；内部逻辑用提前返回快速失败，不用大段捕获来掩盖问题——掩盖就是引入 BUG。
- **资源获取后必须保证释放。** 文件、锁、连接、定时器、事件监听、流等在退出前必须释放，用语言惯用的资源管理方式获取即初始化，不手动获取后靠末尾清理勉强关闭。
- **追求更高效的写法。** 大序列用惰性迭代，不一次性收集到内存；能用语言内建的聚合/映射能力就不手写逐元素循环，数值密集操作用批量/向量化库一次完成。

### 原则五：模块独立，逻辑解耦

每个模块、每条业务线、每条逻辑链在边界上尽量自洽，彼此只通过明确的公共入口通信。解耦的目标是让"改一处"的影响半径最小——这样才不会改一个地方牵出别处的新 BUG。重复是债，过度抽象也是债。

- **只经公共入口通信。** 跨模块调用必须走目标模块的公共入口，不越级深挖内部文件。
- **入口显式暴露对外契约。** 只有公共入口才需要显式列出对外符号；纯内部注册、无对外符号的入口保持空白，不为导出而设空导出列表。
- **保持目录入口干净。** 入口目录只放入口、配置和数据模型；业务逻辑下沉到领域模块。
- **全局量集中在文件头部。** 模块级常量、全局变量在导入声明区下方集中定义，不散落在文件中部或函数之间。
- **共享函数放在最小共同调用者旁。** 当重复真实出现时才抽共享函数，放在最小的调用者旁边，不塞进通用工具模块。
- **提交前先格式化。** 提交前执行 `uv tool run pre-commit run -a`（不要绕过它直接运行单个格式化工具，配置会不一致）。

### 原则六：死代码和历史包袱是炸药

没被调用的代码不是资产，是会让后来者误判的炸药——误以为某条路径还活着、某个兼容入口还需要维护，从而做出错误决策。确认不可达就删，不做"先留着以后有用"。

- **立刻删除死代码。** 无人调用的兼容垫片、零活跃调用者的别名、为不存在的循环依赖风险写的防御性重复导入、永远返回同一值的分支，确认不可达后删掉。
- **与本次无关的死代码报告位置与依据。** 另有清理任务时再删除，不扩大当前请求范围（见原则三）。
- **不写向后兼容的别名。** 重构时直接使用原名并全量重命名，不为减少重命名而写别名。

### 原则七：伙伴相关内容由大模型动态给出

**除拖拽等机械响应外，凡面向用户的伙伴表达都由大模型结合情境、记忆与即时互动推理产生，不靠程序拼装。**

- **不写程序化伙伴文案。** 禁止用时间段、状态枚举或静态模板伪造心情、心理活动、口头反应、主动表达等角色内容。
- **推理结果原样送达呈现层。** 大模型给出的角色内容必须持久化并下发，不丢弃、不改写成模板。
- **缺失时只回退到真实人设。** 尚未收到动态内容时，只展示用户设定的性格等角色定义，不填塞假造语句。
- **机械响应除外。** 拖拽跟随、碰撞回弹等物理或操作反馈由程序兑现，不走大模型。
- **离线/机械降级边界见 DESIGN。** 戳击在 LLM 关闭/冷却/限流时的本地反馈池、拖拽松手本地语音池、仪式行走失败兜底台词属于已文档化的离线与机械降级，不得冒充实时推理结果；媒体与系统提醒使用非人格化通知，不以伙伴第一人称口吻呈现。
